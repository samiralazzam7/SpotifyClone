import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        top: 0,
        width: '80%',
        height: 500,
    },
    searchBar: {
        width: '100%',
        height: 50,
        padding: 10,
        backgroundColor: '#fff',
        fontSize: 20,
        borderRadius: 10,
    },
    essayStyle: {
        padding: 10,
        fontSize: 10,
    },
    results: {
    }
})

export default styles;
